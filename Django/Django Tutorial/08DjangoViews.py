# Django Views
# São funções Python que aceitam HTTP request e retornam HTTP response como documentos HTML.
# Uma página web que usa Django é cheia de views com diferentes tarefas e missões (tasks and missions).
# As views geralmente são colocadas em arquivos chamados views.py e são localizadas no diretório do app.