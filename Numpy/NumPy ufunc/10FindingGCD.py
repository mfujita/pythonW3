# Finding GCD (Greatest Common Denominator) → gcd()
# Calcula o Máximo Divisor Comum
import numpy as np
num1=6
num2=9
print(np.gcd(num1, num2))

# Finding GCD in Arrays → lcm.reduce
array1=np.array([20,8,32,16,36])
print('np.gcd.reduce(array1)')
print(np.gcd.reduce(array1))